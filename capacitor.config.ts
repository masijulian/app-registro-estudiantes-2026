import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.registro.estudiantes',
  appName: 'Registro Estudiantes',
  webDir: 'build'
};

export default config;
